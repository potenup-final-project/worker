name: deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/reusable-build-and-push.yml
    with:
      image_name: potenup/pg-worker
      platforms: linux/amd64
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      GITHUB_PACKAGES_USER: ${{ secrets.GITHUB_PACKAGES_USER }}
      GITHUB_PACKAGES_TOKEN: ${{ secrets.GITHUB_PACKAGES_TOKEN }}

  deploy:
    needs: build
    runs-on: self-hosted
    environment: production

    steps:
      - name: Print image tag
        run: echo "Deploying image tag ${{ needs.build.outputs.image_tag }}"

      - name: Docker login
        run: docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" -p "${{ secrets.DOCKERHUB_TOKEN }}"

      - name: Pull new image
        run: docker pull potenup/pg-worker:${{ needs.build.outputs.image_tag }}

      - name: Stop and remove old container
        run: |
          docker stop pg-worker || true
          docker rm pg-worker || true

      - name: Run new container
        run: |
          docker run -d \
            --name pg-worker \
            --network pg-net \
            --env-file /home/ec2-user/app/pg-worker.env \
            -p 8080:8080 \
            --restart always \
            potenup/pg-worker:${{ needs.build.outputs.image_tag }}

      - name: Verify container running
        run: |
          sleep 5
          docker ps | grep pg-worker

      - name: Health check
        run: |
          for i in {1..10}; do
            if curl -f http://localhost:8080/actuator/health; then
              echo "Service is healthy"
              exit 0
            fi
            echo "Waiting for service..."
            sleep 5
          done
          echo "Service failed to start"
          docker logs pg-worker
          exit 1
